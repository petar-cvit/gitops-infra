apiVersion: cyclops-ui.com/v1alpha1
kind: Module
metadata:
  name: synced
  namespace: cyclops
spec:
  template:
    path: app-template
    repo: https://github.com/cyclops-ui/templates
    version: main
  values:
    general:
      environment:
        ENVIRONMENT: production
      image: nginx
      version: 1.14.2
    name: my-app
    networking:
      expose: true
      port: 80
      type: ClusterIP
    scaling:
      replicas: 2
